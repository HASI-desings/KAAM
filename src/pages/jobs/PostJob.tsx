import { useEffect, useState } from "react";
import { motion } from "framer-motion";
import { Button } from "@/components/ui/Button";
import { PageTransition } from "@/components/ui/PageTransition";
import { Skeleton } from "@/components/ui/Skeleton";
import { supabase } from "@/lib/supabaseClient";
import { useAuth } from "@/hooks/useAuth";
interface CategoryRow { id: string; name: string; }
export default function PostJob({ onPosted }: { onPosted: (jobId: string) => void }) {
 const { session } = useAuth(); const [categories,setCategories]=useState<CategoryRow[]>([]); const [loadingCategories,setLoadingCategories]=useState(true); const [categoryId,setCategoryId]=useState(""); const [title,setTitle]=useState(""); const [description,setDescription]=useState(""); const [paymentType,setPaymentType]=useState<"cash"|"service"|"either">("cash"); const [priceMin,setPriceMin]=useState(""); const [priceMax,setPriceMax]=useState(""); const [deadline,setDeadline]=useState(""); const [criteria,setCriteria]=useState<string[]>([""]); const [posting,setPosting]=useState(false); const [error,setError]=useState<string|null>(null);
 useEffect(()=>{supabase.from("categories").select("id, name").order("name").then(({data})=>{setCategories(data??[]);setLoadingCategories(false);});},[]);
 function updateCriterion(i:number,value:string){setCriteria(c=>c.map((item,idx)=>idx===i?value:item));} function addCriterion(){setCriteria(c=>[...c,""]);}
 async function handlePost(){setError(null);if(!session)return;if(!categoryId)return setError("Choose a category");if(!title.trim())return setError("Title is required");if(!description.trim())return setError("Description is required");if(!deadline)return setError("Deadline is required");if(paymentType!=="service"&&(!priceMin||!priceMax))return setError("Enter a price range");setPosting(true);const {data,error}=await supabase.from("jobs").insert({client_id:session.user.id,category_id:categoryId,title:title.trim(),description:description.trim(),payment_type:paymentType,price_min_cents:paymentType==="service"?null:Number(priceMin)*100,price_max_cents:paymentType==="service"?null:Number(priceMax)*100,deadline:new Date(deadline).toISOString(),acceptance_criteria:criteria.filter(c=>c.trim()),status:"open"}).select("id").single();setPosting(false);if(error)return setError(error.message);onPosted(data.id);}
 return <PageTransition><div className="min-h-screen px-5 py-6"><h1 className="mb-5 text-xl font-bold tracking-tight">Post a Job</h1><div className="space-y-4"><div><label className="mb-1 block text-xs font-medium text-[var(--text-secondary)]">Category</label>{loadingCategories?<Skeleton className="h-11"/>:<select value={categoryId} onChange={e=>setCategoryId(e.target.value)} className="w-full rounded-xl2 border border-[var(--border)] bg-transparent px-3 py-2.5 text-sm outline-none focus:border-teal"><option value="">Select a category</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select>}</div><div><label className="mb-1 block text-xs font-medium text-[var(--text-secondary)]">Title</label><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="e.g. Fix leaking kitchen tap" className="w-full rounded-xl2 border border-[var(--border)] bg-transparent px-3 py-2.5 text-sm outline-none focus:border-teal"/></div><div><label className="mb-1 block text-xs font-medium text-[var(--text-secondary)]">Description</label><textarea value={description} onChange={e=>setDescription(e.target.value)} rows={4} className="w-full resize-none rounded-xl2 border border-[var(--border)] bg-transparent px-3 py-2.5 text-sm outline-none focus:border-teal"/></div><div><label className="mb-1 block text-xs font-medium text-[var(--text-secondary)]">Payment type</label><div className="flex gap-2">{(["cash","service","either"] as const).map(t=><button key={t} onClick={()=>setPaymentType(t)} className={"flex-1 rounded-xl2 border py-2 text-xs font-medium capitalize "+(paymentType===t?"border-teal bg-teal/10 text-teal":"border-[var(--border)] text-[var(--text-secondary)]")}>{t}</button>)}</div></div>{paymentType!=="service"&&<div className="flex gap-3"><div className="flex-1"><label className="mb-1 block text-xs font-medium text-[var(--text-secondary)]">Min price (PKR)</label><input value={priceMin} onChange={e=>setPriceMin(e.target.value.replace(/[^0-9]/g,""))} inputMode="numeric" className="w-full rounded-xl2 border border-[var(--border)] bg-transparent px-3 py-2.5 text-sm outline-none focus:border-teal"/></div><div className="flex-1"><label className="mb-1 block text-xs font-medium text-[var(--text-secondary)]">Max price (PKR)</label><input value={priceMax} onChange={e=>setPriceMax(e.target.value.replace(/[^0-9]/g,""))} inputMode="numeric" className="w-full rounded-xl2 border border-[var(--border)] bg-transparent px-3 py-2.5 text-sm outline-none focus:border-teal"/></div></div>}<div><label className="mb-1 block text-xs font-medium text-[var(--text-secondary)]">Deadline</label><input type="date" value={deadline} onChange={e=>setDeadline(e.target.value)} className="w-full rounded-xl2 border border-[var(--border)] bg-transparent px-3 py-2.5 text-sm outline-none focus:border-teal"/></div><div><label className="mb-1 block text-xs font-medium text-[var(--text-secondary)]">Acceptance criteria</label>{criteria.map((c,i)=><motion.input key={i} initial={{opacity:0}} animate={{opacity:1}} value={c} onChange={e=>updateCriterion(i,e.target.value)} placeholder={"Criterion "+(i+1)} className="mb-2 w-full rounded-xl2 border border-[var(--border)] bg-transparent px-3 py-2.5 text-sm outline-none focus:border-teal"/>)}<button onClick={addCriterion} className="text-xs font-medium text-teal underline underline-offset-2">+ Add another</button></div>{error&&<p className="text-xs text-danger">{error}</p>}<Button className="w-full" loading={posting} onClick={handlePost}>Post Job</Button></div></div></PageTransition>;
}
